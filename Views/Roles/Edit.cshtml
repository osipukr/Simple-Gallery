<div class="row">
    <div class="col-md-8">
        <section id="editRole">
            @using (Html.BeginForm("Edit", "Roles", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                <h4>Редактирование роли</h4>
                <hr />
                @Html.Partial("_Roles")
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Изменить" class="btn btn-default" />
                    </div>
                </div>
            }
        </section>
    </div>
</div>